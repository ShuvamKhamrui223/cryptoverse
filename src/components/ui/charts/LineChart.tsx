import { Line } from "react-chartjs-2";
import { Chart } from "chart.js";
import { CoinsDataType } from "../../../types";
const LineChart = () => {
    const options={}

  return (
    <div>
      {/* <Line options={} data={coinData} /> */}
    </div>
  );
};

export default LineChart;
